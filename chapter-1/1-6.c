#include <stdio.h>

main()
{
    int c;

    while (c != EOF)
    {
        c = (getchar() != EOF)
    }

    /*     c = getchar();
        putchar(c); */
    printf("%d <- this is the print statement\n", c);
    return 0;
}